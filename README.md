# 学术会议投稿截止时间参考

学术会议投稿截止时间展示网站。采用 Vite 构建，支持中英文切换、多时区显示，自动高亮显示即将截止的会议。

## ✨ 特性

- 🔵 **蓝色主题**：清新专业的蓝色渐变配色
- 🌍 **中英文切换**：一键切换界面语言
- ⏰ **多时区支持**：自动适配全球主要时区
- 🚨 **智能提醒**：7天内截止红色闪烁，30天内橙色警告
- 📱 **响应式设计**：完美适配手机、平板、桌面
- 🎯 **分类筛选**：按AI、机器人、ML、CV等分类浏览
- 🔍 **实时搜索**：快速查找会议
- ⏳ **实时倒计时**：每秒更新的倒计时显示
- ⚡ **高性能**：Vite 构建，代码分割，按需加载
- 🎨 **模块化CSS**：组件化样式，易于维护

## 📁 项目结构

```
.
├── index.html                 # 主页面入口
├── package.json              # 项目配置
├── vite.config.js            # Vite 构建配置
├── public/                   # 静态资源
│   └── favicon.svg          # 网站图标
├── src/
│   ├── css/                 # 样式文件
│   │   ├── main.css         # 主样式入口
│   │   ├── variables.css    # CSS 变量定义
│   │   ├── base.css         # 基础样式
│   │   ├── components/      # 组件样式
│   │   │   ├── disclaimer.css
│   │   │   ├── header.css
│   │   │   ├── controls.css
│   │   │   ├── cards.css
│   │   │   └── footer.css
│   │   └── responsive.css   # 响应式样式
│   ├── js/                  # JavaScript 文件
│   │   ├── main.js          # 主入口
│   │   ├── config.js        # 配置与翻译
│   │   ├── state.js         # 状态管理
│   │   ├── utils.js         # 工具函数
│   │   ├── dataService.js   # 数据服务
│   │   ├── renderer.js      # 渲染器
│   │   ├── events.js        # 事件处理
│   │   └── countdown.js     # 倒计时管理
│   └── data/
│       └── conferences.json # 会议数据
└── dist/                     # 构建输出
```

## 🚀 快速开始

### 安装依赖

```bash
npm install
```

### 开发模式

```bash
npm run dev
```

访问 http://localhost:3000

### 生产构建

```bash
npm run build
```

构建输出到 `dist` 目录。

### 预览构建结果

```bash
npm run preview
```

## 🚀 部署到 GitHub Pages

### 自动部署（推荐）

1. **Fork/推送到 GitHub 仓库**

2. **启用 GitHub Pages**
   - 进入仓库 Settings → Pages
   - Source 选择 "GitHub Actions"

3. **自动部署**
   - 每次推送到 `main` 分支会自动触发部署
   - 部署完成后可在仓库的 Environments 中查看状态
   - 网站地址：`https://<username>.github.io/web-ddl/`

### 手动部署

```bash
# 安装 gh-pages
npm install --save-dev gh-pages

# 部署
npm run build
npx gh-pages -d dist
```

## 📝 添加/修改会议

编辑 [`src/data/conferences.json`](src/data/conferences.json) 文件：

```json
{
  "id": "会议唯一标识",
  "name": { "zh": "中文名称", "en": "English Name" },
  "link": "官网链接",
  "description": { "zh": "中文描述", "en": "English Description" },
  "location": { "zh": "举办地点", "en": "Location" },
  "date": { "zh": "会议日期", "en": "Conference Date" },
  "abstractDeadline": "2025-07-25T23:59:59",
  "fullPaperDeadline": "2025-08-01T23:59:59",
  "categories": ["ai", "ml"],
  "tags": ["AI", "Top", "CCF-A"],
  "tagColors": { "AI": "#2196F3", "Top": "#f44336" }
}
```

### 分类选项

| 值 | 中文 | 英文 |
|---|---|---|
| `ai` | 人工智能 | AI |
| `robotics` | 机器人 | Robotics |
| `ml` | 机器学习 | ML |
| `cv` | 计算机视觉 | CV |
| `nlp` | 自然语言处理 | NLP |
| `control` | 控制与决策 | Control |

## 🎨 主题定制

编辑 `src/css/variables.css` 修改变量：

```css
:root {
  --primary-500: #2196f3;  /* 主色 */
  --accent: #00bcd4;        /* 强调色 */
  --urgent: #f44336;        /* 紧急色 */
  --warning: #ff9800;       /* 警告色 */
}
```

## 📊 紧急提醒规则

| 状态 | 颜色 | 说明 |
|---|---|---|
| 🔴 紧急 | 红色 | 7天内截止 |
| 🟠 警告 | 橙色 | 30天内截止 |
| 🟢 正常 | 蓝色 | 30天以上 |
| ⚪ 已截止 | 灰色 | 已过期 |

## 🔧 技术栈

- **构建工具**: Vite 5.x
- **模块化**: ES Modules
- **CSS**: CSS Variables, Grid, Flexbox
- **JavaScript**: Vanilla JS, 模块化架构

## 📜 许可证

MIT